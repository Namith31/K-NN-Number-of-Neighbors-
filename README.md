# K-NN-Number-of-Neighbors-
 Used for both classification and regression problems ● Uses feature similarity to predict values ● Chooses the k nearest neighbors of the new data point and predicts the target value (in case of regression) or target class (in case of classification) accordingly
